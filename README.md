# うさぽんメモ

`usapondays` の「やることリスト」体験を元にした、独立版の小さなメモアプリです。

## 使い方

```bash
npm install
npm run dev
```

ローカル保存には `localStorage` の `usapon_memo_data` を使います。`usapondays` 本体の `usapondays_data` は使いません。

## 機能

- チェックボックス付きメモ作成
- 保存済みメモ一覧
- メモ編集
- アーカイブ
- 削除
